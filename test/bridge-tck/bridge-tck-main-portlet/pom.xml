<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<parent>
		<groupId>com.liferay.faces.test</groupId>
		<artifactId>liferay-faces-test-bridge-tck</artifactId>
		<version>2.1.4-ga5-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>

	<modelVersion>4.0.0</modelVersion>
	<artifactId>bridge-tck-main-portlet</artifactId>
	<name>Bridge TCK Main Portlet</name>
	<packaging>war</packaging>

	<dependencies>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>bridge-tck-compat</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.liferay.faces</groupId>
			<artifactId>liferay-faces-bridge-impl</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>jstl</artifactId>
		</dependency>
		<dependency>
			<groupId>${faces.group.id}</groupId>
			<artifactId>${faces.api.artifact.id}</artifactId>
		</dependency>
		<dependency>
			<groupId>${faces.group.id}</groupId>
			<artifactId>${faces.impl.artifact.id}</artifactId>
		</dependency>
		<dependency>
			<groupId>javax.portlet</groupId>
			<artifactId>portlet-api</artifactId>
		</dependency>
		<dependency>
			<groupId>log4j</groupId>
			<artifactId>log4j</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.myfaces.trinidad</groupId>
			<artifactId>trinidad-impl</artifactId>
			<version>${tck.trinidad.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.myfaces.portlet-bridge</groupId>
			<artifactId>portlet-bridge-tck-main</artifactId>
			<version>${tck.version}</version>
			<type>war</type>
			<exclusions>
				<exclusion>
					<groupId>com.bekk.boss</groupId>
					<artifactId>maven-jetty-pluto-embedded</artifactId>
				</exclusion>
				<exclusion>
					<groupId>javax.faces</groupId>
					<artifactId>jsf-impl</artifactId>
				</exclusion>
				<exclusion>
					<groupId>org.apache.myfaces.portlet-bridge</groupId>
					<artifactId>portlet-bridge-api</artifactId>
				</exclusion>
				<exclusion>
					<groupId>org.apache.myfaces.portlet-bridge</groupId>
					<artifactId>portlet-bridge-impl</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.apache.myfaces.portlet-bridge</groupId>
			<artifactId>portlet-bridge-tck-main</artifactId>
			<version>${tck.version}</version>
			<type>warpath</type>
		</dependency>
	</dependencies>

	<properties>
		<!-- Default Portal: Liferay -->
		<bridge.tck.portal>liferay</bridge.tck.portal>
		<bridge.tck.test.base-url>http://${bridge.tck.test.host}:${bridge.tck.test.port}?p_p_id=58&amp;p_p_lifecycle=0&amp;p_p_state=exclusive&amp;p_p_mode=view&amp;saveLastPath=0&amp;_58_struts_action=%2Flogin%2Flogin</bridge.tck.test.base-url>
		<bridge.tck.test.host>localhost</bridge.tck.test.host>
		<bridge.tck.test.port>8080</bridge.tck.test.port>
		<webapp.descriptor>web-${bridge.tck.portal}-${app.server.type}.xml</webapp.descriptor>
	</properties>

	<profiles>
		<profile>
			<id>pluto</id>
			<properties>
				<bridge.tck.portal>pluto</bridge.tck.portal>
				<bridge.tck.test.base-url>http://${bridge.tck.test.host}:${bridge.tck.test.port}</bridge.tck.test.base-url>
			</properties>
		</profile>
		<profile>
			<id>tck</id>
			<properties>
				<bridge.tck.browser>*firefox</bridge.tck.browser>
				<bridge.tck.selenium.host>localhost</bridge.tck.selenium.host>
				<bridge.tck.selenium.port>4444</bridge.tck.selenium.port>
				<bridge.tck.login.file>src/test/resources/${bridge.tck.portal}-login.properties</bridge.tck.login.file>
				<bridge.tck.test.exclusions.file>target/test-classes/test-exclusions.xml</bridge.tck.test.exclusions.file>
				<bridge.tck.test.file>src/test/resources/${bridge.tck.portal}-tests.xml</bridge.tck.test.file>
			</properties>
			<build>
				<plugins>
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>selenium-maven-plugin</artifactId>
					</plugin>
					<plugin>
						<artifactId>maven-surefire-plugin</artifactId>
					</plugin>
					<plugin>
						<artifactId>maven-dependency-plugin</artifactId>
						<executions>
							<execution>
								<phase>compile</phase>
								<goals>
									<goal>unpack-dependencies</goal>
								</goals>
								<configuration>
									<includeArtifactIds>portlet-bridge-tck-harness</includeArtifactIds>
									<includes>**/*.xml,org/apache/myfaces/portlet/faces/signature/*,org/apache/myfaces/portlet/faces/tckharness/*</includes>
									<outputDirectory>target/test-classes</outputDirectory>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
				<pluginManagement>
					<plugins>
						<plugin>
							<artifactId>maven-surefire-plugin</artifactId>
							<configuration>
								<systemProperties>
									<property>
										<name>bridge.tck.test.file</name>
										<value>${bridge.tck.test.file}</value>
									</property>
									<property>
										<name>bridge.tck.test.exclusions.file</name>
										<value>${bridge.tck.test.exclusions.file}</value>
									</property>
									<property>
										<name>bridge.tck.test.base-url</name>
										<value>${bridge.tck.test.base-url}</value>
									</property>
									<property>
										<name>bridge.tck.login.file</name>
										<value>${bridge.tck.login.file}</value>
									</property>
									<property>
										<name>bridge.tck.selenium.host</name>
										<value>${bridge.tck.selenium.host}</value>
									</property>
									<property>
										<name>bridge.tck.selenium.port</name>
										<value>${bridge.tck.selenium.port}</value>
									</property>
									<property>
										<name>bridge.tck.browser</name>
										<value>${bridge.tck.browser}</value>
									</property>
								</systemProperties>
							</configuration>
						</plugin>
						<plugin>
							<groupId>org.apache.maven.plugins</groupId>
							<artifactId>maven-surefire-report-plugin</artifactId>
							<configuration>
								<outputName>${project.artifactId}-report</outputName>
								<outputDirectory>./target</outputDirectory>
							</configuration>
						</plugin>
						<plugin>
							<groupId>org.codehaus.mojo</groupId>
							<artifactId>selenium-maven-plugin</artifactId>
							<executions>
								<execution>
									<id>start</id>
									<phase>process-test-classes</phase>
									<goals>
										<goal>start-server</goal>
									</goals>
									<configuration>
										<background>true</background>
										<logOutput>true</logOutput>
										<multiWindow>true</multiWindow>
									</configuration>
								</execution>
							</executions>
						</plugin>
						<!--This plugin's configuration is used to store Eclipse m2e settings only. It has no influence on the Maven build itself. -->
						<plugin>
							<groupId>org.eclipse.m2e</groupId>
							<artifactId>lifecycle-mapping</artifactId>
							<version>1.0.0</version>
							<configuration>
								<lifecycleMappingMetadata>
									<pluginExecutions>
										<pluginExecution>
											<pluginExecutionFilter>
												<groupId>org.codehaus.mojo</groupId>
												<artifactId>selenium-maven-plugin</artifactId>
												<versionRange>[2.3,)</versionRange>
												<goals>
													<goal>start-server</goal>
												</goals>
											</pluginExecutionFilter>
											<action>
												<ignore />
											</action>
										</pluginExecution>
									</pluginExecutions>
								</lifecycleMappingMetadata>
							</configuration>
						</plugin>
					</plugins>
				</pluginManagement>
			</build>
			<dependencies>
				<dependency>
					<groupId>com.liferay.faces</groupId>
					<artifactId>liferay-faces-bridge-api</artifactId>
					<version>${project.version}</version>
				</dependency>
				<dependency>
					<groupId>javax.portlet</groupId>
					<artifactId>portlet-api</artifactId>
				</dependency>
				<dependency>
					<groupId>junit</groupId>
					<artifactId>junit</artifactId>
				</dependency>
				<dependency>
					<groupId>org.seleniumhq.selenium.client-drivers</groupId>
					<artifactId>selenium-java-client-driver</artifactId>
				</dependency>
				<dependency>
					<groupId>org.apache.myfaces.portlet-bridge</groupId>
					<artifactId>portlet-bridge-tck-harness</artifactId>
					<version>${tck.version}</version>
				</dependency>
			</dependencies>
		</profile>
		<profile>
			<id>weblogic</id>
			<properties>
				<bridge.tck.test.port>7001</bridge.tck.test.port>
			</properties>
			<dependencies>
				<!-- FACES-1599: Trinidad API must only appear in the global classpath -->
				<dependency>
					<groupId>org.apache.myfaces.trinidad</groupId>
					<artifactId>trinidad-api</artifactId>
					<version>${tck.trinidad.version}</version>
					<scope>provided</scope>
				</dependency>
			</dependencies>
		</profile>
		<profile>
			<id>websphere</id>
			<properties>
				<bridge.tck.test.host>192.168.10.110</bridge.tck.test.host>
				<bridge.tck.test.port>9080</bridge.tck.test.port>
			</properties>
		</profile>
	</profiles>

	<!-- Note that this pom.xml provides similar functionality as the maven-assembly-plugin -->
	<!-- but the "unpack" approach found below meets our needs better than the maven-assembly-plugin -->
	<build>
		<plugins>
			<plugin>
				<groupId>org.appfuse.plugins</groupId>
				<artifactId>maven-warpath-plugin</artifactId>
				<extensions>true</extensions>
				<executions>
					<execution>
						<goals>
							<goal>add-classes</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
